# 快速开始指南

## 5 分钟上手（Chrome 扩展方式）

### 第一步：安装 Python 依赖

```bash
pip install -r requirements.txt
```

### 第二步：加载 Chrome 扩展

1. 打开 Chrome，地址栏输入 `chrome://extensions/`
2. 开启右上角「开发者模式」
3. 点击「加载已解压的扩展程序」
4. 选择项目中的 `chrome-extension/` 目录

加载成功后，工具栏会出现扩展图标。

### 第三步：启动本地服务器

```bash
python server.py
```

看到以下输出说明启动成功：

```
==================================================
  NotebookLM to Anki — Conversion Server
  http://127.0.0.1:5000
==================================================

Ready. Use the Chrome extension to convert quizzes.
```

服务器需要保持运行，不要关闭终端窗口。

### 第四步：提取测验

1. 在 Chrome 中打开 NotebookLM 测验页面
2. 点击工具栏中的扩展图标
3. 弹窗显示「服务器已连接」表示一切就绪
4. 卡组名称会自动填充（也可手动修改）
5. 点击「提取并生成」

扩展会自动提取页面中的测验数据，发送到本地服务器生成 `.apkg` 文件，然后弹出下载对话框。

### 第五步：导入到 Anki

1. 打开 Anki 桌面应用
2. 点击「文件」→「导入」
3. 选择下载的 `.apkg` 文件
4. 点击「导入」

完成！打开卡组即可开始使用交互式选择题卡片复习。

---

## 备选方式：从 HTML 文件提取

适用于无法使用 Chrome 扩展的情况（如使用其他浏览器）。

**1. 保存网页**

在浏览器中打开 NotebookLM 测验页面，按 `Ctrl+S`，保存类型选择「网页，全部」。

**2. 运行主程序**

```bash
python main.py
```

```
请选择操作模式：
1. 启动服务器（推荐）- 配合 Chrome 插件使用
2. 从 HTML 文件提取 - 从已下载的网页文件提取
3. 手动输入 - 手动输入测验题目
4. 退出

请选择 (1-4): 2
```

**3. 输入文件路径**

```
请输入 HTML 文件路径: C:\Users\xxx\Downloads\NotebookLM.html
```

程序会自动查找同目录下的 `_files/shim.html` 并提取测验数据。

**4. 验证和生成**

```
是否逐题验证和编辑？(y/n): y
```

建议选 `y` 逐题检查提取结果，确认无误后生成卡片包。

---

## 备选方式：手动输入

适用于题目较少或其他方式都失败的情况。

```bash
python main.py
# 选择 3
```

```
第 1 题:
问题: Python 中哪个关键字用于定义函数？
选项（输入空行结束选项输入）:
  A. function
  B. def
  C. func
  D.
正确答案 (A/B/C/D...，多选直接连写如 AB): B
解题思路（可选，按 Enter 跳过）: 使用 def 关键字定义函数
点拨内容（可选，按 Enter 跳过）:
所在组（可选，按 Enter 跳过）: Python 基础

继续添加题目？(y/n): n
```

---

## 卡片交互功能说明

导入 Anki 后，卡片支持以下交互操作：

- **点击选项作答**：在正面点击你认为正确的选项
- **翻转查看结果**：背面会显示你的选择和正确答案的对比
- **工具栏按钮**（点击设置齿轮图标展开）：
  - 计时器：开启答题倒计时
  - 随机排序：每次复习时打乱选项顺序
  - 统计：查看累计正确率
  - 定位：显示题目所在组和序号
  - 提示：显示/隐藏答案数量提示

---

## 常见问题

### Q1: 安装依赖时出错

```bash
# 升级 pip
python -m pip install --upgrade pip

# 使用国内镜像
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q2: 扩展弹窗显示「服务器未运行」

确认 `python server.py` 正在运行且终端没有报错。服务器默认使用 `127.0.0.1:5000`，如果端口被占用，可在 `config.py` 中修改 `SERVER_PORT`。

### Q3: 点击「提取并生成」后提示提取失败

1. 确认当前标签页是 NotebookLM 的测验页面
2. 确认测验已完全加载（所有题目和选项都可见）
3. 刷新页面后重试
4. 如果反复失败，使用「HTML 文件提取」方式

### Q4: 如何调整卡片样式？

编辑 `Anki卡片模板/` 目录下的文件：
- `样式.css` — 修改颜色、字体、间距等
- `正面内容模板.html` — 修改正面布局和交互
- `背面内容模板.html` — 修改背面布局和统计逻辑

修改后需要重新生成 `.apkg` 文件并导入。

### Q5: 可以一次处理多个测验吗？

每个测验页面单独提取。生成的 `.apkg` 文件可逐个导入 Anki，或通过设置不同的卡组名称（用 `::` 分隔）组织到不同子卡组中。

---

## 下一步

- 阅读完整的 [README.md](README.md) 了解所有功能
- 编辑 [config.py](config.py) 自定义服务器端口和默认设置
- 修改 `Anki卡片模板/` 中的文件自定义卡片样式
- 查看 [example_quiz_data.json](example_quiz_data.json) 了解数据格式
