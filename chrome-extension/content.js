// Content script — reserved for future use.
// The current approach uses chrome.scripting.executeScript with MAIN world
// from popup.js, which gives direct access to page DOM including iframes.
//
// This file is kept as a placeholder in case we need persistent page-level
// features later (e.g., auto-detect quiz pages, badge updates).
